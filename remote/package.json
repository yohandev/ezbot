{
  "name": "ezbot-remote",
  "version": "1.0.0",
  "description": "Remote control & upload service for the ezbot platform",
  "main": "index.js",
  "author": "Yohan Guyomard",
  "license": "MIT",
  "scripts": {
    "build": "esbuild src/index.js --outfile=www/index.js --bundle --minify --sourcemap --target=chrome89,firefox90,safari15,edge89 --loader:.py=file --loader:.bin=file",
    "serve": "yarn build --watch --serve --servedir=www"
  },
  "devDependencies": {
    "esbuild": "^0.27.4"
  },
  "dependencies": {
    "esptool-js": "0.5.0",
    "jquery": "^4.0.0"
  }
}
